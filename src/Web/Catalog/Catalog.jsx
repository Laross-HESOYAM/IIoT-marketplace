import React from 'react'
import s from './Catalog.module.css'

const Catalog = () => {
  return (
    <div style={{ display: 'flex' }}>
      <div className={`${s.mulDiv1}`}></div>
      <div className={`${s.mulDiv2}`}></div>
    </div>
  )
}

export default Catalog
